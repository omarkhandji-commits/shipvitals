# Next.js adapter

Check App Router vs Pages Router, route handlers, middleware auth, server/client component boundaries, environment variables, image optimization, metadata, loading/error states, API routes, and `next build`. For UI projects, require mobile and desktop proof before `READY`.
