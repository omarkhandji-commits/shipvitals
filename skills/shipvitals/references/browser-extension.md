# Browser Extension Gate

- manifest is valid;
- permissions are minimal;
- content scripts are scoped;
- no broad host permissions without reason;
- popup/options pages work;
- extension handles missing DOM elements gracefully;
- store listing assets are honest;
- privacy disclosures match behavior.
