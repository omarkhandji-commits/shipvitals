# Quick audit mode

Use quick mode when the project already has a config and the user wants a fast release check.

Run static scans, project commands that are already known, and a short falsification pass. Mark runtime, visual, and independent review as `NOT VERIFIED` unless they were actually executed.
