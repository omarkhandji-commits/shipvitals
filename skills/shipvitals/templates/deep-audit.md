# Deep audit mode

Use deep mode for launch, client delivery, marketplace submission, or paid work.

Required steps: load product promise, execute configured commands, scan for secrets and fake completion, run or request visual/runtime proof for UI projects, classify findings with severity rules, produce a final report, and include exact retest commands.
