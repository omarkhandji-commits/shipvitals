# Session example: DEMO ONLY

ShipVitals finds mock data and fake integrations in the promised core flow. It returns `DEMO ONLY`, not `ALMOST READY`, because the project is acceptable for a demo but not real users.
