# Session example: ALMOST READY

ShipVitals finds no P0 blockers, but finds missing mobile proof and one failed non-critical test. It returns `ALMOST READY`, gives a short fix plan, and includes exact retest commands.
