# Session example: READY

User asks for a deep audit. ShipVitals loads `.shipvitals-config.json`, runs build/lint/test, checks secrets, verifies no fake-completion signals, records visual evidence as provided, and returns `READY` with no P0/P1 findings.
